<%= form_with(model: select_category) do |form| %>
  <% if select_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(select_category.errors.count, "error") %> prohibited this select_category from being saved:</h2>

      <ul>
        <% select_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :category_id %>
    <%= form.select :category_id,Category.all.map { |category|[category.name, category.id]},{},class: "form-control" %>
  </div>
<br>
<div class="d-grid gap-2">
    <%= form.submit '登録',class: "btn btn-primary" %>
</div>
<% end %>
