
<div class = "container">
  <div class = "row">
    <div class = "col-md-3"></div>
      <div class = "col-6">

  <p id="notice"><%= notice %></p>
  <h1>ホーム</h1>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @chats.each do |chat| %>
      <tr>
        <td><%= link_to image_tag(url_for(image_of_groups_path(chat.group.id)),size:'100x100'), group_path(chat.group.id) %></td> 
        <td><%= link_to chat.group.name, chat_path(id:chat.id,group_id:chat.group.id) %></td>
        <td><%= link_to 'Destroy', chat, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%# <%= link_to'プロフィール設定',profile_path(current_user.id)%>
<%= link_to 'ほかのグループに参加', groups_path %>
</div>

